import { Angular2GoogleChartDirective } from './angular2-google-chart.directive';

describe('Angular2GoogleChartDirective', () => {
  it('should create an instance', () => {
    const directive = new Angular2GoogleChartDirective();
    expect(directive).toBeTruthy();
  });
});
